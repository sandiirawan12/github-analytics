# GitHub Analytics — Environment Variables
# Copy to .env for local runs. Actions uses secret GH_PAT (do not commit .env).

GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional if username is set in analytics.config.yml (or leave both empty in Actions → repo owner)
# GITHUB_USERNAME=your-username

# TIMEZONE=UTC
# OUTPUT_DIR=./output
